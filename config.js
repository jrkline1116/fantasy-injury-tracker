// Fill these in during setup (README steps 2 and 3). All three are safe to publish:
// the anon/publishable key only allows what the database's row-level security permits,
// and the VAPID public key is meant to be public.
window.FIT_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT-REF.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-OR-PUBLISHABLE-KEY",
  VAPID_PUBLIC_KEY: "YOUR-VAPID-PUBLIC-KEY",
  BMC_URL: "https://buymeacoffee.com/jrkline1116",
};
